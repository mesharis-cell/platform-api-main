/**
 * Hybrid Pricing System - Seed Data
 * (Transport rates and vehicle types removed from schema - seed skipped)
 */

export async function seedHybridPricing() {
    console.log("⏭️  Hybrid pricing seed skipped (transport rates and vehicle types removed)");
}
